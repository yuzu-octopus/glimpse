# 002 — Perf Fixes

Base: `e2c784d`. Depends on 001. Status: TODO.

## Why

Perf audit top wins: idle preload-all defeats code-split; full-page 30s/1s poll; unbounded warmup fan-out; TTL skew; render storm per chunk.

## Tasks

1. **Preload scope** (`src/client/widgets/index.ts`, `main.tsx`): preload visible-page widgets only on idle; drop global preload-all. Verify: Network panel shows ≤ page widgets on load.
2. **Per-widget polling** (`src/shared/live.ts`, `usePageData.ts`): poll live widgets individually (homelab 1s, network 30s) instead of full-page refetch. Verify: unrelated widgets don't re-render on tick.
3. **Warmup cap** (`src/server/warmup.ts`): bound concurrency (e.g. pLimit 6), keep all-settled + guarded reload. Verify: boot log shows capped batches.
4. **TTL align** (`vite.config.ts`, `cache.ts`, `usePageData.ts`): document single TTL table; align SW 300s vs client 30s vs server 60s–1h per widget kind. Verify: no double-fetch on stale boundary.
5. **Fonts + memo** (`src/server/index.ts` serveDist immutable fonts; memoize WidgetChrome list rows). Verify: font 304s, no extra renders in React DevTools.

Files out of scope: fetchers, theme, config loader.

## Done

tsc clean · tests green · doctor 100/100 · manual: cold load faster, polling quiet, boot bounded. If per-widget polling complicates reconcile, STOP and keep page poll with render-skip instead.
