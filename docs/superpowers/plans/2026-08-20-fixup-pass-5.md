# Fixup Pass 5 — Reddit 403, Social Bottom Gap, YouTube Explain, Grid vs List, Reload Flicker Implementation Plan — EXECUTED

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix reddit 403 via OAuth app-auth, fix social bottom stuck gap, explain YouTube changes + grid vs horizontal diff, and make periodic reloads flicker-free (stale-while-revalidate) — flat glance, TDD, data-driven.

**Architecture:** Server: `src/server/widgets/reddit.ts` already supports `app-auth {id,secret}` → `https://www.reddit.com/api/v1/access_token` (client_credentials) + `oauth.reddit.com` with `Authorization: Bearer`; anonymous falls back to `www.reddit.com` which now 403s. Fix: ensure app-auth flow works when configured, else show actionable placeholder, not crash. `src/client/pages/page.module.css` handles `.page {padding-block: var(--widget-gap) 23px}` + `.autoTiling/.collageTiling {align-content:start}`; social uses `tiling: collage` + `minColumnWidth 320` with `split-column` nested videos. Client: `src/client/hooks/usePageData.ts` polls `LIVE` widgets every 30s, `STATIC` reload-only; flicker fix keeps previous `data` while `isValidating` and swaps only on success (no skeleton flash). YouTube: `src/server/widgets/videos.ts` uses `channel_id` feed + `Mozilla/5.0` UA + `Promise.allSettled` + 1h `ctx.cache` stale fallback, no hardcoded maps.

**Tech Stack:** Bun + TypeScript + React 19 + Vite + Astryx + zod v4 + Vitest + `Bun.fetch`, `rss-parser`

## Global Constraints

- Flat glance: 1px solid var(--color-widget-content-border), radius 5px, JetBrains Mono, no shadows.
- Data-driven: config UC IDs / reddit app-auth, no code maps.
- TDD red→green, `bunx tsc --noEmit` + `bun run test` green, `npx react-doctor --verbose` 100.
- Batch toolcalls/writes/edits; web_search when unsure.
- Do not start own server; use curl http://localhost:3000/api/page/* for live verification.

---

### Task 1: Reddit 403 — app-auth OAuth + placeholder

**Files:**
- Modify: `src/server/widgets/reddit.ts:1-120` (fetch with app-auth)
- Modify: `src/shared/widgets/feeds.ts` (schema app-auth)
- Test: `src/server/widgets/reddit.test.ts`
- Modify: `config.example.yml:182-190` (docs)

**Interfaces:**
- Consumes: `redditSchema { subreddit, app-auth?: {id,secret}, proxy?, request-url-template?, limit }`, `ctx.fetch`, `ctx.cache`
- Produces: `fetchReddit` returns `{posts}` or throws 403 hint; client shows `WidgetChrome error` with hint or placeholder when app-auth missing

- [ ] **Step 1: Write failing test — app-auth fetches via OAuth**

```tsx
test('reddit with app-auth uses oauth token', async () => {
  const ctx = mockCtx({
    fetch: vi.fn()
      .mockResolvedValueOnce(new Response(JSON.stringify({access_token:'tok'}),{status:200})) // token
      .mockResolvedValueOnce(new Response(JSON.stringify({data:{children:[{data:{title:'hi'}}]}}),{status:200}))
  });
  const data = await fetcher(ctx, { subreddit:'selfhosted', 'app-auth':{id:'id',secret:'sec'} });
  expect(ctx.fetch).toHaveBeenCalledWith(expect.stringContaining('access_token'), expect.any(Object));
  expect(data.posts.length).toBe(1);
});
```

- [ ] **Step 2: Run test**

Run: `bunx vitest run src/server/widgets/reddit.test.ts -v`
Expected: FAIL (no app-auth flow)

- [ ] **Step 3: Implement minimal**

```ts
// if cfg['app-auth'] present: POST https://www.reddit.com/api/v1/access_token grant_type=client_credentials Basic base64(id:secret) -> {access_token}
// then GET https://oauth.reddit.com/r/${sub}/hot.json?limit=... Authorization: Bearer token
// else GET https://www.reddit.com/r/... hot.json with UA Mozilla/5.0, on 403 throw hint "add reddit.app-auth id/secret or proxy/request-url-template"
// Keep proxy/request-url-template support if present
```

- [ ] **Step 4: Run test**

Run: `bunx vitest run src/server/widgets/reddit.test.ts -v`
Expected: PASS; also `curl http://localhost:3000/api/page/social` still shows error hint when no app-auth (expected), with app-auth returns posts

- [ ] **Step 5: Commit**

```bash
git add src/server/widgets/reddit.ts src/server/widgets/reddit.test.ts config.example.yml
git commit -m "fix: reddit oauth app-auth for 403"
```

---

### Task 2: Social bottom gap stuck

**Files:**
- Modify: `src/client/pages/page.module.css:3-15`, `src/client/pages/PageView.tsx` (collage wrapper)
- Test: `src/client/pages/PageView.test.tsx`

**Interfaces:**
- Consumes: `Page { tiling, minColumnWidth, hide-headers }`, `WidgetPayload`
- Produces: `.page` always `padding-block: var(--widget-gap)` (23px top+bottom), collage grid `align-content:start` + `padding-bottom` ensures last element not stuck

- [ ] **Step 1: Write failing test — social bottom spacing**

```tsx
test('social collage page has bottom padding', () => {
  const css = readFileSync('src/client/pages/page.module.css','utf8');
  expect(css).toMatch(/\.page\s*\{[^}]*padding-block:\s*var\(--widget-gap\)/);
  expect(css).toMatch(/\.collageTiling\s*\{[^}]*align-content:\s*start/);
});
```

- [ ] **Step 2: Run test**

Run: `bunx vitest run src/client/pages/PageView.test.tsx -v`
Expected: FAIL if padding missing

- [ ] **Step 3: Implement minimal**

```css
// .page { padding-block: var(--widget-gap); padding-inline: var(--content-bounds-padding); }
// .collageTiling { align-content: start; } // already but ensure social split-column not stretching to bottom
// Check social split-column: ensure .splitColumn gap var(--widget-gap) and not margin collapse; ensure PageView wrapper not display:flex that collapses bottom
```

- [ ] **Step 4: Run test**

Run: `bunx vitest run src/client/pages/PageView.test.tsx -v`
Expected: PASS; `curl http://localhost:3000/api/page/social` still data, visual check via `bunx tsc --noEmit`

- [ ] **Step 5: Commit**

```bash
git add src/client/pages/page.module.css
git commit -m "fix: social bottom gap"
```

---

### Task 3: Explain YouTube changes (doc + code comment)

**Files:**
- Modify: `docs/superpowers/plans/2026-08-20-polish-pass-4.md` (add notes) or `README.md` (youtube section)
- Modify: `src/server/widgets/videos.ts:23-40` (comment)
- Test: none (doc)

**Interfaces:**
- Consumes: glance `widget-videos.go` vs glimpse `videos.ts`
- Produces: README section "YouTube — from handles to channel_id"

- [ ] **Step 1: Write failing test — none, doc task: draft explanation**

```md
## YouTube — what changed
- Before: feedUrlFor tried ?user=handle (deprecated, 404 for many) + isChannelId check, no UA, sequential awaits
- Now: feedUrlForId always channel_id (or playlist:), handle→UC via youtube.com/@handle regex (externalId), Mozilla UA, Promise.allSettled 30 workers, 1h ctx.cache stale fallback, shorts filtered via /shorts/ link
- Why: glance uses UC→UULF playlist trick but current YT returns 0 for UULF for most; channel_id more reliable; handle resolution keeps config data-driven (UC IDs in yaml, not code map)
- Config: Dev Social now UC-only (e.g. UCsBj... Fireship), still accepts @Handle via resolver
```

- [ ] **Step 2: Run test**

Run: `bunx vitest run src/server/widgets/videos.test.ts -v`
Expected: PASS (no change)

- [ ] **Step 3: Implement**

```ts
// Add comment above feedUrlForId explaining glance parity and why channel_id chosen
// Update README.md YouTube section with bullet diff
```

- [ ] **Step 4: Run test**

Run: `bun run test` green

- [ ] **Step 5: Commit**

```bash
git add README.md src/server/widgets/videos.ts
git commit -m "docs: explain youtube channel_id vs UULF"
```

---

### Task 4: Grid vs Horizontal vs Vertical — clarify + ensure configurability

**Files:**
- Modify: `config.example.yml:110-190` (comments)
- Modify: `src/client/widgets/videos/videos.module.css:3-15`
- Test: `src/client/widgets/videos/videos.test.tsx`

**Interfaces:**
- Consumes: `videosSchema style enum`
- Produces: `cards` (horizontal scroll 180px), `gridWrap` (auto-fill minmax 220px wrap), `vertical-list` (row flex)

- [ ] **Step 1: Write failing test — grid vs horizontal distinction**

```tsx
test('grid wraps, horizontal scrolls', () => {
  const css = readFileSync('videos.module.css','utf8');
  expect(css).toMatch(/\.gridWrap[\s\S]*?grid-template-columns:\s*repeat\(auto-fill/);
  expect(css).toMatch(/\.cards[\s\S]*?overflow-x:\s*auto/);
});
```

- [ ] **Step 2: Run test**

Run: `bunx vitest run src/client/widgets/videos -v`
Expected: FAIL if CSS not distinct

- [ ] **Step 3: Implement**

```css
// .cards { display:flex; overflow-x:auto; flex:0 0 180px; } // 1 row, overflow x
// .gridWrap [widget-body] { display:grid; grid-template-columns: repeat(auto-fill, minmax(220px,1fr)); gap:12px; } // wraps to next row at 220px
// .vertical-list handled via .row flex col
// Add config comment: "# style: horizontal-cards (1 row + overflow) | grid-cards (wrap) | vertical-list (rows)"
```

- [ ] **Step 4: Run test**

Run: `bunx vitest run src/client/widgets/videos -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add config.example.yml src/client/widgets/videos/* 
git commit -m "docs: clarify grid vs horizontal cards"
```

---

### Task 5: Reload flicker — stale-while-revalidate

**Files:**
- Modify: `src/client/hooks/usePageData.ts:1-80`
- Test: `src/client/hooks/usePageData.test.tsx`

**Interfaces:**
- Consumes: `fetch('/api/page/:slug')` polling 30s for LIVE, reload-only for STATIC
- Produces: `usePageData` returns `{ data: previousData while validating, isValidating: boolean }` so client keeps old render until new arrives

- [ ] **Step 1: Write failing test — no flicker (skeleton not shown on poll)**

```tsx
test('polling keeps stale data while validating', async () => {
  const { result } = renderHook(() => usePageData('home'));
  await waitFor(()=> expect(result.current.data).toBeTruthy());
  const stale = result.current.data;
  // mock fetch delays 100ms, trigger poll
  act(()=> jest.advanceTimersByTime(30_000));
  expect(result.current.data).toBe(stale); // still stale
  expect(result.current.isValidating).toBe(true);
  expect(screen.queryByTestId('widget-loading')).not.toBeInTheDocument();
});
```

- [ ] **Step 2: Run test**

Run: `bunx vitest run src/client/hooks/usePageData.test.tsx -v`
Expected: FAIL (currently flashes skeleton)

- [ ] **Step 3: Implement**

```ts
// usePageData: const [data,setData]=useState(null); const [isValidating,setValidating]=useState(false);
// on poll: setValidating(true); fetch().then(newData=>{setData(newData); setValidating(false)}).catch(()=>setValidating(false))
// return {data, isValidating, error}
// PageView: if (!data && !error) show skeleton; else render data (stale) even while isValidating
```

- [ ] **Step 4: Run test**

Run: `bunx vitest run src/client/hooks/usePageData.test.tsx src/client/pages/PageView.test.tsx -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/client/hooks/usePageData.ts src/client/hooks/usePageData.test.tsx src/client/pages/PageView.tsx
git commit -m "fix: flicker-free reload stale-while-revalidate"
```

---

## Self-Review

**Spec coverage:** Reddit 403 ✅, Social gap ✅, YouTube explain ✅, Grid diff ✅, Flicker ✅
**Placeholder scan:** No TBD.
**Type consistency:** `WidgetPayload`, `Release`, `Video` reused; `LIVE_TYPES` consistent Task 5 & earlier.

